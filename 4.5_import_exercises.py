from functionsexercises import *


is_two(2)

is_consonant("E")

is_vowel("E")

import functionsexercises

functionsexercises.is_consonant("F")


from functionsexercises import is_consonant as iscon

import itertools as it
it.product(("abcd", '123'))
it.product('ABCD', repeat=2)
import json
import re
from collections import defaultdict, Counter
with open("profiles.json", "r") as read_file:

[profile for profile in profile if profile['isActive']]

[profile ["isActive"] for profile in profile ]